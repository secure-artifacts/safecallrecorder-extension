# 人工测试（简化版）

1. 打开 VoiceMeeter，声音进入 Out B1。
2. 点击扩展图标，打开简洁管理页。
3. 如需授权，点“授权设备”。
4. 选择 VoiceMeeter Out B1，确认音浪跳动。
5. 可选填写“VK通话”，比特率选 64 kbps。
6. 开始录音至少 30 秒，确认当前时长与已安全保存增加。
7. 停止录音，确认显示正在生成 MP3，并自动下载。
8. 文件名类似：`0823_VK通话.mp3`（日期默认不带年份、中间无横杠；可在名称设置里改成 `20260823`）。
9. 播放 MP3 有声音；历史中仍保留并可再次下载。
10. 重启浏览器后历史仍在；删除需二次确认。
